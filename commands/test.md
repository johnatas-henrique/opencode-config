---
description: "Run tests with coverage and report results. Use this agent to execute test suites, gather coverage data, and provide insights on test performance and code quality."
agent: build
model: "opencode/big-pickle"
---

Run the full test suite with coverage report and show any failures or issues found. 
Focus on the failing tests and provide insights on what might be causing the failures. If all tests pass, provide a summary of the test coverage and any areas that might need improvement.
