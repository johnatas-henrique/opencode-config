---
description: Generate a new title for this session and apply it automatically
---

Analyze the conversation history of this session and generate a concise, descriptive title (3-7 words) that captures the main topic.

After generating the title, immediately call the `session-rename` tool with that title to update the session name.

Do not ask for confirmation — just rename automatically.

Return a brief confirmation message to the user.
